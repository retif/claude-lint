# claude-lint configuration
# All rules are enabled by default. Override here to disable or change severity.
#
# Usage:
#   Disable a rule:
#     plugin-json/no-unknown-fields: false
#
#   Change severity:
#     skill-md/body-word-count:
#       enabled: true
#       severity: info
#
# Available severities: error, warning, info

rules: {}
